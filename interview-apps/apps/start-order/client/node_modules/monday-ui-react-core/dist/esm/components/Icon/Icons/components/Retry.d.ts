import * as React from 'react';
export interface RetryProps extends React.SVGAttributes<SVGElement> {
    size?: string | number;
}
declare const Retry: React.FC<RetryProps>;
export default Retry;
