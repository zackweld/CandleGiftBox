import { FC } from 'react';
import { ElementContent } from '../../types';
type FunctionArgumentsProps = {
    children: ElementContent;
};
declare const FunctionArguments: FC<FunctionArgumentsProps>;
export default FunctionArguments;
