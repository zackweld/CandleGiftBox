import * as React from 'react';
export interface HealthProps extends React.SVGAttributes<SVGElement> {
    size?: string | number;
}
declare const Health: React.FC<HealthProps>;
export default Health;
