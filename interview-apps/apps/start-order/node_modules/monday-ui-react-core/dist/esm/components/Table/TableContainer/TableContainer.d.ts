import React from "react";
import { TableContainerProps } from "./TableContainer.types";
declare const TableContainer: ({ id, className, "data-testid": dataTestId, style, children }: TableContainerProps) => React.JSX.Element;
export default TableContainer;
