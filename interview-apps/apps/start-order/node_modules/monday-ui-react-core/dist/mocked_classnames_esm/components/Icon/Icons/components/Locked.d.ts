import * as React from 'react';
export interface LockedProps extends React.SVGAttributes<SVGElement> {
    size?: string | number;
}
declare const Locked: React.FC<LockedProps>;
export default Locked;
