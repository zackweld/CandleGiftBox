import React from 'react';
type Required<T> = {
    [P in keyof T]-?: T[P];
};
export declare const withStaticProps: <T, S>(forwarded: React.FC<T> & Partial<S>, staticProps: Required<S>) => React.FC<T> & Partial<S> & Required<S>;
export {};
