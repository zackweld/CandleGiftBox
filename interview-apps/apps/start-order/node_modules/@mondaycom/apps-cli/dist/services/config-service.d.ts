import { ConfigData, InitConfigOptions } from '../types/services/config-service.js';
export declare const CONFIG_NAME = ".mappsrc";
export declare const ConfigService: {
    checkConfigExists: (directoryPath: string, fileName?: string) => boolean;
    checkLocalConfigExists: (fileName?: string) => boolean;
    getConfigDataByKey(key: keyof ConfigData): string;
    loadConfigToProcessEnv(directoryPath: string, fileName?: string): Partial<{
        accessToken: string;
    }>;
    removeConfig(directoryPath: string, fileName?: string): boolean;
    init(data: ConfigData, directoryPath: string, options?: InitConfigOptions): Partial<{
        accessToken: string;
    }> | undefined;
};
