import { AppId } from '../types/general/index.js';
import { AppVersion } from '../types/services/app-versions-service.js';
export declare const listAppVersionsByAppId: (appId: AppId) => Promise<Array<AppVersion>>;
export declare const defaultVersionByAppId: (appId: AppId, useLiveVersion?: boolean) => Promise<AppVersion | undefined>;
